{
  "watch": "src",
  "ext": "ts, json",
  "ignore": ["src/**/*.spec.ts"],
  "env": {
    "NODE_ENV": "development"
  },
  "exec": "ts-node -r dotenv/config src/index.ts dotenv_config_path=./.env.dev"
}
